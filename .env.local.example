# Copy this file to .env.local and fill in values.
# NEVER commit .env.local to git.

# 32+ char random secret used to sign /command access tokens.
# Generate: openssl rand -hex 32
COMMAND_SECRET=replace_with_32_char_random_string

# Optional: override the default command token for dev
# COMMAND_TOKEN=dev-token-for-local-testing
